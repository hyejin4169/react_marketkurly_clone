import React from "react";
import styled from "styled-components";

const Main = () => {
  return (
    <Container>
    </Container>
  );
};

const Container = styled.div`
  text-align: center;

  svg {
    width: 8rem;
    height: 8rem;
  }
`;

export default Main;